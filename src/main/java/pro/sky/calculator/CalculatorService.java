package pro.sky.calculator;

public interface CalculatorService {

    String hello();

    Integer calculatePlus(Integer a, Integer b);

    Integer calculateMinus(Integer a, Integer b);

    Integer calculateMultiply(Integer a, Integer b);

    Integer calculateDivide(Integer a, Integer b);
}
